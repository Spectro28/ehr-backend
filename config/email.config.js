module.exports = {
  email: {
    host: 'smtp.gmail.com', // Servidor SMTP de Gmail
    port: 587, // Puerto estándar para SMTP con STARTTLS
    secure: false, // Indica si se usará una conexión segura directa (false = STARTTLS, true = SSL/TLS)
    auth: {
      user: 'systemehr76@gmail.com', // Usuario del servidor SMTP (tu correo)
      pass: 'avgr pgwe xypj hbvb' // Contraseña de aplicación generada en Gmail
    }
  }
};